# Auto-generated by make_minimal_zip.py
# Source: .env

# ===== Database =====
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DATABASE_URL=

# ===== Auth / Default Admin =====
# 首次启动会自动创建该管理员（若数据库里还没有同名username）
DEFAULT_ADMIN_NAME=
DEFAULT_ADMIN_USERNAME=
DEFAULT_ADMIN_PASSWORD=

# JWT / Cookie（登录态）
# 说明：登录态通过 HttpOnly cookie 保存 JWT；默认“保持登录”7天（可改）。
APP_SECRET_KEY=
TOKEN_EXPIRE_HOURS=
REMEMBER_EXPIRE_DAYS=
# 生产环境(HTTPS)建议 1；本地 http 开发建议 0，否则浏览器不会发送 cookie
COOKIE_SECURE=

# ===== AI (通过你的 Relay 转发) =====
# 例如：http://www.marllen.com:4000/v1
OPENAI_BASE_URL=
OPENAI_API_KEY=

OPENAI_TIMEOUT=
OPENAI_MODEL=
OPENAI_RETRIES=
OPENAI_REASONING_EFFORT=

# ===== Marllen 小助手（可选：走本机 Codex）=====
# 默认：openai（仍走 OPENAI_BASE_URL 的 /v1/responses）
# 可选：codex（通过本机 clawd-relay 调用 `codex exec`）
MARLLEN_ASSISTANT_UPSTREAM=openai

# 仅排障时开启：把“上游请求 payload / codex 命令与耗时”等写入 AssistantJob.extra
MARLLEN_ASSISTANT_SAVE_TRACE=

# --- Codex via clawd-relay（当 MARLLEN_ASSISTANT_UPSTREAM=codex 时需要） ---
# Docker 容器访问 macOS 宿主机服务：host.docker.internal
CLAWD_RELAY_BASE_URL=http://host.docker.internal:333
# 机密：从 /Users/<you>/.clawdbot/credentials/clawd-relay.json 取 execToken（不要提交到仓库）
CLAWD_RELAY_EXEC_TOKEN=

# Codex 工作区（默认：客服质检项目）
MARLLEN_ASSISTANT_CODEX_CWD=/Users/marllenos/customor-qc-app
# 可选：指定 codex 使用的模型（留空=走 codex 默认/配置）
MARLLEN_ASSISTANT_CODEX_MODEL=
# 安全默认：read-only（也可 workspace-write / danger-full-access）
MARLLEN_ASSISTANT_CODEX_SANDBOX=read-only
# 可选：强制走本机开源模型提供方（需要 Ollama/LM Studio 正在运行）
MARLLEN_ASSISTANT_CODEX_OSS=
# 可选：ollama / lmstudio
MARLLEN_ASSISTANT_CODEX_LOCAL_PROVIDER=

# ===== Bucket import (S3 compatible) =====
# 你现在的目录结构类似：
#   conversations-marllen-taobao / leyan / 2026-01-19 / 2208....jsonl.gz
# Cron 会在每天 10:15 轮巡“昨天”这一天的目录；若未抓到则每小时重试，并通过飞书 webhook 提醒。
#
# 只需要把 key / endpoint / bucket 填上即可（不要把密钥提交到仓库）。

# （已弃用）是否启用淘宝/抖音抓取：以 UI【设置 > 桶抓取/导入】保存的开关为准
ENABLE_TAOBAO_BUCKET_IMPORT=
ENABLE_DOUYIN_BUCKET_IMPORT=

# 每天轮巡时间（上海时间）
BUCKET_DAILY_CHECK_TIME=

# 本地备份目录（容器内路径；docker-compose 已把 ./data 挂载到 /data）
BUCKET_BACKUP_DIR=

# 飞书提醒（你自己填）
FEISHU_WEBHOOK_URL=

# --- TAOBAO / Aliyun OSS (S3 compatible) ---
# Bucket 名称（例如：conversations-marllen-taobao）
TAOBAO_BUCKET_NAME=
# S3 endpoint（阿里 OSS：例如 https://oss-cn-shanghai.aliyuncs.com ）
TAOBAO_S3_ENDPOINT=
# region（可留空）
TAOBAO_S3_REGION=
TAOBAO_S3_ACCESS_KEY=
TAOBAO_S3_SECRET_KEY=
# 顶层前缀（你这里是 leyan ）
TAOBAO_BUCKET_PREFIX=

# --- DOUYIN / Aliyun OSS (S3 compatible) ---
# 若抖音与淘宝共用同一 OSS 桶/同目录上传：DOUYIN_* 可留空，系统会自动复用 TAOBAO_* 配置。
DOUYIN_BUCKET_NAME=
DOUYIN_S3_ENDPOINT=
DOUYIN_S3_REGION=
DOUYIN_S3_ACCESS_KEY=
DOUYIN_S3_SECRET_KEY=
DOUYIN_BUCKET_PREFIX=
